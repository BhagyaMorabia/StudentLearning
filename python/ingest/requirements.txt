# NeuralJEE Python Ingestion Pipeline
# Requirements for: Gemini content generation, BGE embeddings, DB push

# Gemini API (Google AI)
google-genai

# BGE-base embeddings (local, free, 768-dim)
sentence-transformers
torch  # CPU-only, no CUDA required for inference

# Math validation
sympy

# Database connectivity
psycopg2-binary

# Utilities
python-dotenv    # Load .env file
tqdm             # Progress bars
